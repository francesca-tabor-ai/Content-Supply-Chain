export function Playbook() {
  return (
    <div>
      <header className="page-header">
        <div className="accent-bar" aria-hidden />
        <h1 className="h1">Digitised Playbook Builder</h1>
        <p className="subtitle">
          Create playbooks from the global template, auto-populate with approved masters, and publish to Brand Central in one click.
        </p>
      </header>
      <section className="card">
        <h2 className="card-title">Version history</h2>
        <p className="card-desc">
          Version history and diff view for all playbook iterations. No manual upload or version management.
        </p>
      </section>
    </div>
  )
}

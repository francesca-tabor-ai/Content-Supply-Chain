{
  "name": "content-supply-chain",
  "private": true,
  "version": "0.1.0",
  "description": "CSC Dashboard — Workfront, Brand Central, StudioOne integration",
  "scripts": {
    "dev:api": "pnpm --filter @csc/api dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm -r build",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
